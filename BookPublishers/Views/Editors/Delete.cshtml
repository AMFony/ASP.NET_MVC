@model BookPublishers.Models.Editor
@{
    ViewBag.Title = "Delete";
}
<div class="row">
    <div class="col-12">
        <h3>Confirm Delete</h3>
    </div>
    <div class="form-group row mb-1">
        @Html.LabelFor(x => x.EditorName, new { @class = "col-form-label col-form-label-sm col-2" })
        <div class="col-4">
            @Html.DisplayFor(x => x.EditorName, new { @class = "form-control form-control-sm" })

        </div>
    </div>
    <div class="form-group row mb-1">
        @Html.LabelFor(x => x.MobileNo, new { @class = "col-form-label col-form-label-sm col-2" })
        <div class="col-4">
            @Html.DisplayFor(x => x.MobileNo, new { @class = "form-control form-control-sm" })

        </div>
    </div>
    <div class="form-group row mb-1">
        @using (Html.BeginForm())
        {
            @Html.HiddenFor(x => x.EditorId)

            <div class="col-4 offset-2">
                <button type="submit" class="btn btn-danger">Confirm Delete</button>
            </div>
        }
    </div>
</div>




